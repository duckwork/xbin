#!/bin/dash

. $WMRC
ARGS=""

gwc cur "$PFW";

test $# -ne 0 && e="$@" || e=lsw;
$e | grep -vx "$PFW" | \
while read wid; do
    gwc win $wid;
    test $(( winy + winh )) -lt $cury && continue;
    test $(( cury + curh )) -lt $winy && continue;
    echo $wid;
done
